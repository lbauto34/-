1. 对于下面的C语言声明描述正确的一项是（）C

    char (*p)[16]


   A p是长度为16的字符指针数组

   B p是包含16个字符的字符串

   C p是指向长度为16的字符数组的指针

   D p是长度为16的字符数组
   
解答：p和谁先结合，p就是什么。
[]的运算级别大于*，
1、char *p[16]，根据运算优先级，[]优于*，所以p和[]先结合，因此p是数组；
2、char (*p)[16]，p和*由括号括住，p和*先结合，因此p是 指针 。
    
    
